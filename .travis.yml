language: generic

before_install:
  - mkdir -p $HOME/bin
  - curl -s https://testspace-client.s3.amazonaws.com/testspace-linux-dev.tgz | tar -zxvf- -C $HOME/bin
  - testspace -v

script:
  - echo script
   
after_script:
  - ./trigger-builds.sh
